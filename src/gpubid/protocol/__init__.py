"""Negotiation protocol: broadcast -> eligibility -> rounds -> budget -> HITL.

Phases 5 (broadcast/eligibility/round) and 9 (HITL) are scaffolded with TODO
markers; Phase 6 (budget) is implemented in full because it has no LLM
dependency and is shared by every variant of the protocol.
"""
